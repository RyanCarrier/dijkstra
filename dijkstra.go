package dijkstra

//Shortest calculates the shortest path from src to dest
func (g *Graph) Shortest(src, dest int64) (int64, error) {
	return 0, ErrNoPath
}

//Longest calculates the longest path from src to dest
func (g *Graph) Longest(src, dest int64) (int64, error) {
	return 0, ErrNoPath
}
